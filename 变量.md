

# 一、变量类型

## 1.整数类型：
>int：用于表示整数，通常占用4个字节。  
short：用于表示短整数，通常占用2个字节。  
long：用于表示长整数，通常占用4个字节。  
long long：用于表示更长的整数，通常占用8个字节。  

## 2.浮点类型：
>float：用于表示单精度浮点数，通常占用4个字节。  
double：用于表示双精度浮点数，通常占用8个字节。  

## 3.字符类型：
>char：用于表示字符，通常占用1个字节。  

## 4.布尔类型：
>bool：用于表示布尔值，只能取true或false。

## 5.枚举类型：
>enum：用于定义一组命名的整数常量。

## 6.指针类型：
>type*：用于表示指向类型为type的对象的指针。

## 7.数组类型：
>ype[ ]或type[size]：用于表示具有相同类型的元素组成的数组。

## 8.结构体类型：
>struct：用于定义包含多个不同类型成员的结构。

## 9.类类型：
>class：用于定义具有属性和方法的自定义类型。

## 10.共用体类型：
>union：用于定义一种特殊的数据类型，它可以在相同的内存位置存储不同的数据类型。

# 二、变量定义与声明

## 1.声明是用来告诉编译器变量的名称和类型，而不分配内存。
定义是为了给变量分配内存，可以为变量赋初值。  
可以理解为定义是加了初始化(赋予某一对象一个值，在此次赋值前该对象默认值为0)的声明
## 2.表示方法
(1)声明：数据类型 变量名 ；
```
int    a, b, c;
char   n, m;
float  j, k;
double d;
```
(2)定义：数据类型 变量名 = 变量初始值；
```
int a = 3, b = 5; 
char n = 'n';
float j = 2.2
```
其中变量名被我们称为标识符，是可以自己随便起的，但要注意符合下面几条规则：  
### 1.由字母、数字及下划线组成，且不能以数字开头  
### 2.不能与关键字同名  
<img width="1440" height="811" alt="image" src="https://github.com/user-attachments/assets/96ba8e94-b6ad-4139-8ce4-a68e2172cc12" />

### 3.大写字母和小写字母代表不同意义（如a6和A6即为两个不同的标识符）
